#pragma once

/// @file drivers.hpp
/// @brief Hardware drivers behind reusable interfaces (Phase 2+).
///
/// Drivers interface directly with hardware and are consumed by
/// services. Modules never access drivers directly.

namespace gallus::drivers {}
